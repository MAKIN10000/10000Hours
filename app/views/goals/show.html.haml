%h1 #{@goal.title} 

%ul#details
  %li
    Owner: #{@goal.owner}
%h3 Description:

%p#description= @goal.description



= button_to 'Delete', goal_path(@goal), :method => :delete, :confirm => 'Are you sure?'
